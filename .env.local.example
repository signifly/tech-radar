# Copy to .env.local and fill in. .env.local is gitignored — never commit real tokens.

# DatoCMS read API token. Without it, the app falls back to content/blips.ts.
DATOCMS_API_TOKEN=

# Optional: include draft (unpublished) blips — handy in preview environments.
# DATOCMS_DRAFT=true
